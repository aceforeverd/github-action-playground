#include "example.h"

int main()
{

    // this is a line that should be more than 80 characters, water water water water water babababa
    example::PlayGround pg(0, "lol");
    pg.play();
    return 0;
}
